# Troy · Piqua (troy-piqua) — EIA consumer-energy / grid / demand-pressure outputs

Per-site EIA reference outputs for the Troy · Piqua watershed point (basin: great-miami). Values
come from the `watermark.economics.energy` / `watermark.grid` connectors keyed to this site's
`SiteProfile` in `watermark.sites` — nothing here is fabricated; regenerate, don't hand-edit.

## Files

| File | What | Source |
|---|---|---|
| `consumer-energy.yaml` | Ohio residential electricity price (¢/kWh), residential natural-gas price ($/Mcf), and total electricity retail sales (million kWh), each as a provenance-tagged annual point series. | US **EIA API v2** `seriesid` route — `ELEC.PRICE.OH-RES.A` (2001–2025), `ELEC.SALES.OH-ALL.A` (2001–2025), `NG.N3010OH3.A` (1967–2025). `source: connector`. Regenerated by `watermark eia`. |
| `grid-profile.yaml` | The serving electric utility (**Dayton Power & Light / AES Ohio**, EIA-861 #4922; the City of Piqua municipal utility #15095 serves the Piqua share) + balancing authority (**PJM**) identification and the EIA-861/930 utility/BA profile. Regenerated by `watermark grid`. | Serving utility + BA: EIA-861 2024 service-territory file (`source: reference`). Utility/BA profile: EIA-861 per-utility (retail/customers/price) + EIA-930 annual (PJM), `source: connector`. |
| `demand-pressure.yaml` | The disclosed "Project Klondike" facility's electricity demand sized against Ohio consumer prices — annual consumption, share of state retail sales, households-equivalent, and a stylized price-pressure band (#1482). Regenerated by `watermark eia`, facility-gated. | Derived — `PowerBasis.facility_draw` (`watermark.facility.power`, #87, from `SiteProfile.facility`'s floor-area screening IT-load bracket) sized against `consumer-energy.yaml`'s state retail sales + residential price. |

## Known gaps & caveats

- **`grid-profile.yaml`'s `load_share` is `null`.** It was generated before the site's facility was
  disclosed (#1482) and has not been regenerated (`watermark grid --write`) to express the campus
  load as a share of the utility/BA/state denominators — the same trailing gap Urbana's own
  `grid-profile.yaml` still carries after #1327.
- **EIA requires a key** (`WATERMARK_EIA_API_KEY`, free). Without one, `watermark eia` replays the
  committed offline fixtures (`tests/fixtures/economics/eia/`); a live keyed pull refreshes to the
  latest annual vintage. Figures here are EIA **annual averages** (latest stable annual; EIA annual
  data lags ~1 year) — the committed 2025-vintage pull (2026-07-11/2026-07-12).
- **The campus buys at wholesale/industrial rates, not the residential price** shown in
  `demand-pressure.yaml` — that price is the *consumer-impact reference*, not the campus's own bill.
- **`demand-pressure.yaml`'s IT load is an `[inference]` floor-area screening bracket** (52.5 / 113.75
  / 175 MW), not a disclosed permit load — see `data/extracted/troy-piqua/data-centers.md` and
  `it_load_citation` on `SiteProfile.facility`. The price-pressure band is a **stylized screening
  sensitivity, not a forecast**.

## Regenerate

`watermark --site troy-piqua eia --write` (consumer energy + demand pressure) and
`watermark --site troy-piqua grid --write` (grid profile).
