{
  "$defs": {
    "ConsumerEnergyPrice": {
      "additionalProperties": false,
      "description": "One EIA consumer energy-price (or sales) series, with its full annual history.\n\nA consumer-level energy figure for the state/region — residential electricity\nprice, residential natural-gas price, or total electricity retail sales — read\nfrom the EIA API v2. ``value``/``period`` are the latest point (a convenience for\ncallers that only need the current figure); ``points`` is the full annual series\n(oldest→newest) so the site can chart the trend (issue #1111). ``value`` carries its\nnative units (cents/kWh, $/Mcf, or million kWh for the sales series); ``source: connector``.",
      "properties": {
        "series_id": {
          "title": "Series Id",
          "type": "string"
        },
        "label": {
          "title": "Label",
          "type": "string"
        },
        "fuel": {
          "title": "Fuel",
          "type": "string"
        },
        "metric": {
          "default": "price",
          "title": "Metric",
          "type": "string"
        },
        "period": {
          "title": "Period",
          "type": "string"
        },
        "area": {
          "title": "Area",
          "type": "string"
        },
        "value": {
          "$ref": "#/$defs/ProvenancedValue"
        },
        "points": {
          "default": [],
          "items": {
            "$ref": "#/$defs/EnergyPricePoint"
          },
          "title": "Points",
          "type": "array"
        }
      },
      "required": [
        "series_id",
        "label",
        "fuel",
        "period",
        "area",
        "value"
      ],
      "title": "ConsumerEnergyPrice",
      "type": "object"
    },
    "EnergyPricePoint": {
      "additionalProperties": false,
      "description": "One annual point on an EIA energy series: ``period`` + native-unit ``value``.\n\nDeliberately compact. Provenance is carried once at the series level\n(:class:`ConsumerEnergyPrice`: ``series_id``, ``value.unit``, connector source) rather\nthan repeated on every point — each point's citation is deterministically\n``EIA API v2 seriesid {series_id} ({period})`` — so the full annual history (25-60 pts)\nstays a tiny fixture (issue #1111). The latest point is additionally exposed as the\nfully-cited ``ConsumerEnergyPrice.value`` for callers needing one provenanced figure.",
      "properties": {
        "period": {
          "title": "Period",
          "type": "string"
        },
        "value": {
          "title": "Value",
          "type": "number"
        }
      },
      "required": [
        "period",
        "value"
      ],
      "title": "EnergyPricePoint",
      "type": "object"
    },
    "ProvenancedValue": {
      "additionalProperties": false,
      "description": "A single numeric quantity tagged with its provenance.\n\nConstruct via the classmethods (:meth:`from_document`, :meth:`from_connector`,\n:meth:`assume`, :meth:`derived`) so the ``source`` tag is never forgotten.",
      "properties": {
        "value": {
          "title": "Value",
          "type": "number"
        },
        "unit": {
          "title": "Unit",
          "type": "string"
        },
        "source": {
          "enum": [
            "document",
            "connector",
            "reference",
            "assumption",
            "derived"
          ],
          "title": "Source",
          "type": "string"
        },
        "citation": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Citation"
        },
        "confidence": {
          "default": "medium",
          "enum": [
            "high",
            "medium",
            "low"
          ],
          "title": "Confidence",
          "type": "string"
        },
        "asof": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Asof"
        }
      },
      "required": [
        "value",
        "unit",
        "source"
      ],
      "title": "ProvenancedValue",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Committed EIA consumer energy-cost reference for the state/region (issue #91).\n\nThe consumer-price half of the demand thread: what households pay for electricity\nand heating fuel, against which the data-center load's pressure is screened. A\nvendored, regenerable reference (``watermark eia``) like the QCEW baseline — the site\nreads the committed YAML, never a live pull. Every figure is connector-sourced.",
  "properties": {
    "area": {
      "title": "Area",
      "type": "string"
    },
    "area_name": {
      "title": "Area Name",
      "type": "string"
    },
    "prices": {
      "items": {
        "$ref": "#/$defs/ConsumerEnergyPrice"
      },
      "title": "Prices",
      "type": "array"
    },
    "source": {
      "default": "US EIA API v2 (seriesid route): residential prices + retail sales",
      "title": "Source",
      "type": "string"
    },
    "note": {
      "default": "",
      "title": "Note",
      "type": "string"
    }
  },
  "required": [
    "area",
    "area_name",
    "prices"
  ],
  "title": "ConsumerEnergyCosts",
  "type": "object",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
