{
  "$defs": {
    "ContractorAward": {
      "additionalProperties": false,
      "description": "A USASpending federal-award record joined to a matched corpus entity (#1662, ME-C).\n\nThe dollars are verbatim from ``data/reference/usaspending/[<slug>/]awards.yaml``; the join\nkey is the entity node's ``uei`` (stamped by the same watchlist), so the feed's federal totals\nreconcile with the entity graph rather than re-deriving them.",
      "properties": {
        "entity_key": {
          "title": "Entity Key",
          "type": "string"
        },
        "recipient_name": {
          "title": "Recipient Name",
          "type": "string"
        },
        "uei": {
          "title": "Uei",
          "type": "string"
        },
        "total_obligations": {
          "title": "Total Obligations",
          "type": "number"
        },
        "nexus": {
          "title": "Nexus",
          "type": "string"
        },
        "defense_share": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Defense Share"
        },
        "annual_obligations": {
          "items": {
            "$ref": "#/$defs/FederalAnnualFlow"
          },
          "title": "Annual Obligations",
          "type": "array"
        },
        "by_psc": {
          "items": {
            "$ref": "#/$defs/FederalCategory"
          },
          "title": "By Psc",
          "type": "array"
        },
        "by_naics": {
          "items": {
            "$ref": "#/$defs/FederalCategory"
          },
          "title": "By Naics",
          "type": "array"
        }
      },
      "required": [
        "entity_key",
        "recipient_name",
        "uei",
        "total_obligations",
        "nexus"
      ],
      "title": "ContractorAward",
      "type": "object"
    },
    "DefenseContractorItem": {
      "additionalProperties": false,
      "description": "A seed prime defense contractor + the corpus entities its patterns matched.\n\nWhen a matched entity resolves to a USASpending recipient, its awards are stamped here (#1662):\n``awards`` carries the per-entity federal records, and ``total_obligations`` / ``nexus`` roll\nthem up (the summed dollars + the strongest nexus). A contractor with no matched award keeps\nan empty ``awards`` and null totals.\n\n``tag`` types the discipline the page previously carried only as a prose callout (#1663, ME-D):\nit is the register of the item's **corridor-presence claim**, not of the seed row (the seed list\nis curated, and a prime that matched nothing still ships so the search is visible). A bare\n``matched_entities`` hit is a case-insensitive substring match on a party name — a lead, so\n``inference``; a match corroborated by a UEI-pinned award whose curated ``nexus`` is\n``verified`` earns ``verified``; nothing matched leaves the question standing, so ``open``.",
      "properties": {
        "name": {
          "title": "Name",
          "type": "string"
        },
        "note": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Note"
        },
        "patterns": {
          "items": {
            "type": "string"
          },
          "title": "Patterns",
          "type": "array"
        },
        "matched_entities": {
          "items": {
            "type": "string"
          },
          "title": "Matched Entities",
          "type": "array"
        },
        "awards": {
          "items": {
            "$ref": "#/$defs/ContractorAward"
          },
          "title": "Awards",
          "type": "array"
        },
        "total_obligations": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Total Obligations"
        },
        "nexus": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Nexus"
        },
        "tag": {
          "default": "open",
          "enum": [
            "verified",
            "inference",
            "reference",
            "open"
          ],
          "title": "Tag",
          "type": "string"
        },
        "tag_basis": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Tag Basis"
        }
      },
      "required": [
        "name"
      ],
      "title": "DefenseContractorItem",
      "type": "object"
    },
    "FederalAnnualFlow": {
      "additionalProperties": false,
      "description": "One federal fiscal year's prime-award obligations (the annual flow, #1662).",
      "properties": {
        "fiscal_year": {
          "title": "Fiscal Year",
          "type": "integer"
        },
        "obligations": {
          "title": "Obligations",
          "type": "number"
        }
      },
      "required": [
        "fiscal_year",
        "obligations"
      ],
      "title": "FederalAnnualFlow",
      "type": "object"
    },
    "FederalCategory": {
      "additionalProperties": false,
      "description": "One PSC/NAICS category's share of a recipient's federal obligations (#1662).",
      "properties": {
        "code": {
          "title": "Code",
          "type": "string"
        },
        "name": {
          "title": "Name",
          "type": "string"
        },
        "obligations": {
          "title": "Obligations",
          "type": "number"
        }
      },
      "required": [
        "code",
        "name",
        "obligations"
      ],
      "title": "FederalCategory",
      "type": "object"
    },
    "ScanParcel": {
      "additionalProperties": true,
      "description": "A parcel row from the defense-land GIS scan (extra GIS columns allowed).\n\nTwo registers travel with one row, and conflating them is the failure this model exists to\nprevent (#1663, ME-D). The GIS columns — owner, situs, acres, value — are verbatim from the\ncounty's public parcel service, so ``record_tag`` is ``verified``. What the scan *claims the\nparcel is* — a named prime's holding, the Joint Systems Manufacturing Center — is an analyst\nreading over those columns, carried in ``attribution`` and tagged separately by\n``attribution_tag``. Before this, the ``[inference]`` marker on the JSMC identification lived\nonly as a free-text prefix inside the scan's ``meta.army_controlled_note`` prose, so a consumer\ncould not tell a verified ownership row from an inferred attribution without parsing English.",
      "properties": {
        "record_tag": {
          "default": "verified",
          "enum": [
            "verified",
            "inference",
            "reference",
            "open"
          ],
          "title": "Record Tag",
          "type": "string"
        },
        "attribution": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Attribution"
        },
        "attribution_tag": {
          "default": "inference",
          "enum": [
            "verified",
            "inference",
            "reference",
            "open"
          ],
          "title": "Attribution Tag",
          "type": "string"
        },
        "attribution_basis": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Attribution Basis"
        }
      },
      "title": "ScanParcel",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "The defense-contractors feed: the seed list + the parcel-scan findings.",
  "properties": {
    "contractors": {
      "items": {
        "$ref": "#/$defs/DefenseContractorItem"
      },
      "title": "Contractors",
      "type": "array"
    },
    "prime_owned": {
      "items": {
        "$ref": "#/$defs/ScanParcel"
      },
      "title": "Prime Owned",
      "type": "array"
    },
    "army_controlled": {
      "items": {
        "$ref": "#/$defs/ScanParcel"
      },
      "title": "Army Controlled",
      "type": "array"
    },
    "notes": {
      "additionalProperties": true,
      "title": "Notes",
      "type": "object"
    }
  },
  "title": "DefenseFeed",
  "type": "object",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
