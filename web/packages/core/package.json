{
  "name": "@watermark/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runtime-agnostic BOSC domain logic (feeds, catalog, sdm, storyCompile, revalidate, mcpTools, readiness, evidence, dilution, …) — DOM-free and React-free so it imports cleanly from the Astro site, the Cloudflare Pages Functions (Workers runtime), and vitest. Epic #1549, Phase 1 (#1551).",
  "exports": {
    "./*": "./src/*.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "check": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "remark-directive": "^4.0.0",
    "remark-mdx": "^3.1.1",
    "remark-parse": "^11.0.0",
    "unified": "^11.0.5",
    "unist-util-visit": "^5.1.0",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/geojson": "^7946.0.16",
    "@types/hast": "^3.0.5",
    "@types/node": "^22.19.20",
    "@types/unist": "^3.0.3",
    "typescript": "^5.7.0",
    "vfile": "^6.0.3"
  }
}
