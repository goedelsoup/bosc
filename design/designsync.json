{
  "_comment": "Two-way DesignSync config — see DESIGNSYNC.md. The Claude Design project is the source of truth; this repo mirrors it under localDir. Used as the target + localDir for the DesignSync MCP tool.",
  "projectId": "dbe30a08-547c-442e-b4ac-81492fa5570f",
  "name": "Watermark Design System",
  "localDir": "design",
  "synced": [
    "SKILL.md",
    "readme.md",
    "styles.css",
    "tokens/colors.css",
    "tokens/fonts.css",
    "tokens/typography.css",
    "tokens/spacing.css",
    "components/core/**",
    "components/forms/**",
    "components/record/**",
    "components/charts/**",
    "components/hydrology/**",
    "components/navigation/**",
    "templates/directory/**",
    "templates/site/**",
    "templates/design-spec/**",
    "guidelines/**",
    "kits/data-catalog/**"
  ],
  "pending": [
    "assets/brand/**",
    "templates/watermark-platform/**"
  ],
  "generated": [
    "_ds_bundle.js",
    "_ds_manifest.json",
    "_adherence.oxlintrc.json",
    "kits/data-catalog/support.js",
    "templates/design-spec/support.js",
    "guidelines/contributing/support.js"
  ],
  "notes": {
    "synced": "Foundations + all component specs (components/{core,forms,record,charts,hydrology,navigation}/** + templates/{directory,site}/**) + guideline cards are mirrored. 2026-07-02 pull: upstream restructured — ui_kits/* -> templates/*, templates/data-catalog -> kits/data-catalog, templates/contributing -> guidelines/contributing — mirror follows (git mv). New upstream: components/navigation/** (Chrome, the global ink bar) and templates/design-spec/** (DesignSpec.dc.html — the issue #760 ProvenancedValue-ranges brief) pulled; ds-base.js per-template loader mirrored, support.js dc-runtime bundles are generated (not mirrored). NOTE: content of previously-synced files was not re-diffed on the 2026-07-02 structural pull — a content pass is a follow-up.",
    "pending": "assets/brand/** (intentionally — see below) and templates/watermark-platform/** (11 full-page .dc.html mocks incl. Sustainability + Chrome, added upstream 2026-07; large — pull page-by-page as each is needed). NOT mirrored (design-canvas artifacts, not components): explorations/**, kits/social-kit/**, uploads/**. On a future pull, the exact paths come from DesignSync list_files.",
    "generated": "Authored by the Claude Design app's self-check / tooling — never mirror or push these.",
    "assets/brand/**": "Brand binaries already live in web/public/ (favicons, app icons); only mirror here if you want them version-controlled in the design system too."
  }
}
